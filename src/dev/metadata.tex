% metadata control
% \usepackage[owner=Donald,user=Knuth,print=false]{pdfcrypt} %! only works with pdfLaTeX
% \usepackage{xmpincl} %! only works with pdfLaTeX
\usepackage{hyperxmp} %! must be loaded before the hyperref package %! mess with authblk; the reason however remains unclear, and it still occurs when hyperxmp is suppresses
% \usepackage{popupmenu} %! not supported by Overleaf
% \usepackage{threadcol,repltext} %! pdfLaTex and LuaLaTeX only
\usepackage{media9}
% \usepackage{cooltooltips,pdfprivacy} %! only works with pdfLaTeX
% \usepackage{attachfile,attachfile2} %! only works with pdfLaTeX; severe incompatibility
% \usepackage{embedfile,embedall} %! only works with pdfLaTeX