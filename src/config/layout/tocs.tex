% tables of content
%! the titletoc package conflicts with XeTeX engine; use tocloft instead
\usepackage{tocloft} % specify the "titles" options if you combine it with the titlesec package
\usepackage{etoc}
\usepackage{shorttoc}
% \usepackage{minitoc} % must load after caption packages; moved to caption.tex