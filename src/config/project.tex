\usepackage{excludeonly} % provide \excludeonly{} which works with \include{}
\usepackage{import} % handy when relative imports are needed; but look at the documentation please
% \usepackage{docmute} % this can used in simple multi-file projects: ignores anything outside the document environment 
\usepackage[subpreambles=true,print,sort]{standalone}
\usepackage{changebar}