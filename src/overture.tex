%! commands to be called before \documentclass{}; \usepackage will cause error; use \RequirePackage instead
\RequirePackage{silence}
    \input{src/settings/set-silence}
% \input{src/dev/diagnose} % if use, must put it here
% \syntaxonly % fast debugging (not supported by Overleaf!)