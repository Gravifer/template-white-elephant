% bibliography referencing
%! this template assumes the biblatex system and the biber backend
% \usepackage{natbib} %! biblatex is way better
% \usepackage{cite} %! incompatible with biblatex
\usepackage[backend=biber, natbib=true, backref=false, url=false]{biblatex} % requires using the \addbibresource{} command to add .bib files
% \usepackage[citestyle=verbose,bibstyle=subseries]{biblatex}

% \usepackage{multibib} % makes it possible to make multiple bibliographies; must use together with the bibtex engine