%! allows siaplay of algorithms and source codes
% for algorithms and pseudo-codes
% \usepackage{algorithmic} %  a bit of a legacy, but an IEEE default
% \usepackage{algorithm} %! provides a floating algorithm environment; use together with algorithmic; must be loaded after hyperref!!!
\usepackage[linesnumbered,ruled,vlined]{algorithm2e} % more mordern than algorithmic; seems as good as it can get for LaTeX2e
% \usepackage{algpseudocode} % the algorithmicx package? can be used with the algorithm package
% \usepackage{program} % seems unsupported by Overleaf
% for real source codes
\usepackage{listings} % provides display of various programming languages
% \usepackage[framed,numbered]{mcode} % provides direct import of MatLab codes (based on listings)(not supported by overleaf)
